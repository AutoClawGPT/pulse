export default function Loading() {
  return (
    <div className="flex min-h-[100dvh] items-center justify-center font-[family-name:var(--font-mono)] text-[var(--muted)]">
      bringing the tape up
    </div>
  );
}
